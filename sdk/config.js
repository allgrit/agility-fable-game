// Конфиг игры для Fable Arcade SDK. Единственный файл, которым владеет игра.
// sdk-sync его не перезаписывает.

export const SDK = {
  // Уникальный id игры в общем бэкенде (реестр — README SDK).
  GAME_ID: 'agility-trial',

  // Версия сборки. Показывается в меню, служит меткой аналитики.
  VERSION: 'v1 · 2026-07-11',

  // Общий бэкенд Fable Arcade (лидерборд + аналитика).
  API: 'https://tribe.tsdpu.org/lb',

  // Секрет подписи результатов (HMAC), общий для всех игр. Подпись целостности
  // (виден в коде), а не крипто-защита — поднимает порог накрутки.
  SECRET: 'a45652a4af5b603f00f3dc8d9346135cc2fb73a3da209686',

  // Имя кэша Service Worker. У этой игры свой network-first SW (js/main.js),
  // держим имя синхронно с его BUILD ради контроля версий.
  CACHE: 'agility-trial-v1',
};
